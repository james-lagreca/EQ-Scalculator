name = "scalculator"
version = "1.0.0"
requires-python = ">=3.9"
description = "Interactive earthquake-scaling calculator"

dependencies = [
  "streamlit>=1.32",
  "pandas>=2.0",
  "numpy>=1.23",
  "plotly>=5.18",      # ← NEW
  "openpyxl>=3.1"      # ← NEW  (xlsx reader)
]

[project.optional-dependencies]
dev = ["pytest", "black", "ruff"]